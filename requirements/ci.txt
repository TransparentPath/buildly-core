-r base.txt

bandit==1.7.8
coverage==7.4.4
factory_boy==3.3.0
flake8==7.0.0
# Pinned deliberately: these were unpinned, so every run re-resolved them from
# PyPI and the suite's result changed with the calendar rather than with a commit.
# pytest-django >= 4.12 requires Django >= 5.2 (it reads
# PytestDjangoTestCase._pre_setup_ran_eagerly), while base.txt pins Django~=5.0.0.
pytest==8.4.2
pytest-django==4.11.1
pytest-cov==5.0.0
httpretty==1.1.4
# NOTE: pytest-asyncio 1.x removed the `event_loop` fixture, which
# gateway/tests/test_views_async.py still requests at 16 sites, so those 10 tests
# error at collection. Pinned to the currently-resolved version to freeze the
# known baseline; migrating those tests off `event_loop` is tracked separately.
pytest-asyncio==1.4.0
